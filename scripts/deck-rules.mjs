/**
 * Правила колоды переехали в `src/shared/lib/deckRules.js`: их спрашивает
 * не только этот скрипт, но и форма, в которой человек заводит карточку
 * руками. Две копии одних правил разъехались бы.
 *
 * Файл оставлен, чтобы `check-deck.mjs` и его проверки не переписывать.
 */
export * from '../src/shared/lib/deckRules.js';
